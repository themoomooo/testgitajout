


class Ball {
    constructor(x, y, width, height, color, radius){
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        this.color = color;
        this.radius = radius;
    }

    drawing() {
       
    
       this.style.top=`${draw.x}px`;
    this.style.left=`${draw.y}px`;
        this.style.width=`${draw.width}px`;
        this.style.height=`${draw.height}px`;
        this.style.color=`${draw.radius}px`;
        balle.style.radius=`${draw.x}px`;
    
        
        
    }
    
}


const draw = new Ball(50, 150,20,20,'red',10);
console.log(draw);

function 
drawing();